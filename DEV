- to profile the twb:

    make pnc to generate gprof profiling output
    then gprof gmon.out

    make pbc to generate ocamlprof output
    then ocamlprof <filename.ml>

- to compile the twb in native code
    
    make nc
    the default is byte-code
    
